/**
 * Allows <code>ThreeWayDiff</code> to operate on <code>JsonElement</code>s. These are not added by default, and 
 * so have to be set manually. These use the List and Map implementations in the background, simply converting between 
 * <code>JsonArray</code> and <code>JsonObject</code> and Array and Map as needed.
 * @author leo
 *
 */
package com.crypticbit.javelin.diff.jsonelement;