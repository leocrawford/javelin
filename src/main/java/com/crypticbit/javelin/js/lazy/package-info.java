/**
 * Implement Collections (currently Map and List) such that their elements (which are other Collections, or values) are 
 * stored as references which are dereferenced  on-demand (lazily)
 */

package com.crypticbit.javelin.js.lazy;