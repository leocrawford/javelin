package com.crypticbit.javelin.js;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import org.junit.Assert;
import org.junit.Test;

public class UnorderedIndexedWritesListDecoratorTest {

    private static final String[] SIMPLE_ARRAY = new String[] { "a", "b", "c", "d" };
    /*
     * @Test public void testAddAllIntCollection() { List<String> t = new UnorderedIndexedWritesListDecorator<>( new
     * ArrayList<String>(Arrays.asList(SIMPLE_ARRAY))); t.addAll(0, Arrays.asList(new String[]{"x","y","z"}));
     * t.addAll(2, Arrays.asList(new String[]{"u"})); t.addAll(1, Arrays.asList(new String[]{})); t.addAll(1,
     * Arrays.asList(new String[]{"s","t"})); t.addAll(2, Arrays.asList(new String[]{"v"})); System.out.println(t);
     * Assert.assertArrayEquals(new String[]{"x","y","z","a","s","t","b","u","v","c","d"},t.toArray()); }
     * @Test public void testAdd() { List<String> t = new UnorderedIndexedWritesListDecorator<>( new
     * ArrayList<String>(Arrays.asList(SIMPLE_ARRAY))); t.add(0, "u"); t.add(0, "v"); t.add(1, "w"); t.add(1, "x");
     * t.add(2, "y"); t.add(1, "z"); System.out.println(t); Assert.assertArrayEquals(new
     * String[]{"u","v","a","w","x","z","b","y","c","d"},t.toArray()); }
     * @Test public void testRemove() { List<String> t = new UnorderedIndexedWritesListDecorator<>( new
     * ArrayList<String>(Arrays.asList(SIMPLE_ARRAY))); t.remove(1); t.remove(2); Assert.assertArrayEquals(new
     * String[]{"a","d"},t.toArray()); }
     */
}
